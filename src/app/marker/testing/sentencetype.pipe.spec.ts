import { SentencetypePipe } from '../sentencetype.pipe';

describe('SentencetypePipe', () => {
  it('create an instance', () => {
    const pipe = new SentencetypePipe();
    expect(pipe).toBeTruthy();
  });
});
